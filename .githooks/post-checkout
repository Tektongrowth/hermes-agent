#!/usr/bin/env bash
set -euo pipefail
HOOK_DIR=$(cd "$(dirname "$0")" && pwd)
source "$HOOK_DIR/lib/active-branch-guard.sh"
if [[ "${3:-0}" == "1" ]]; then
  require_active_branch "branch checkout"
fi
